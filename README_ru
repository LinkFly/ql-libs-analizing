Библиотека для анализа состояния quicklisp-библиотек.

Тестировано с Quicklisp версией 2012081400 (с последующими тоже должна работать).

Упоминающийся здесь "рейтинг" системы, это число систем которые зависят от данной.
В данный момент система экспортирует 4 функции: 

1. Получить хэш таблицу в которой ключами являются строки, представляющие 
ASDF-системы, а значениями числа представляющие рейтинги:

	(create-rating-hash) => <хэш-таблица>

2. Получить отсортированный по возрастанию массив, элементы в котором 
представляют список вида (<рейтинг> "имя_системы"):

	(get-sorted-ratings &key (test #<)) => <массив-списков> 

3. Получить подмножество массива рейтингов, в котором рейтинги меньше или равны
max-rating:

	(get-subset-rating (create-sorted-ratings) max-rating)

4. Напечатать в поток вывода списки вида (<рейтинг> <имя-системы>), где рейтинг
больше или равен max-rating:

   	(print-ratings (&optional (max-rating 0))
		    	       	    


				    
